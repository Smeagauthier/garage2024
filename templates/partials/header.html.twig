<nav class="navbar navbar-expand-lg custom-navbar" style="background-color: #1E1E1E;">
    <div class="container-fluid d-flex align-items-center">
        <div class="navbar-brand mr-5">
            <a href="{{ path('homepage') }}"><img src="{{ asset('uploads/logo-garage.png') }}" alt="Logo" class="logo" style="width: 130px; height: 130px;"></a>
        </div>
        <div class="navbar-nav d-flex flex-grow-1 mx-5">
            <a href="{{ path('homepage') }}" class="nav-link text-uppercase fw-bold text-white me-4">Accueil</a>
            <a href="{{ path('voitures') }}" class="nav-link text-uppercase fw-bold text-white me-4">Showroom</a>
        </div>
        <div>
            <a href="#" class="nav-link text-uppercase fw-bold text-white me-5">Inscription</a>
        </div>
    </div>
</nav>